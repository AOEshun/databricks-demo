resources:
  pipelines:
    dlt_staging:
      name: dlt-staging-azurestorage
      channel: CURRENT

      target: ${var.catalog}.STAGING_AZURESTORAGE

      configuration:
        catalog: ${var.catalog}

      libraries:
        - notebook:
            path: staging/dlt/04_staging_dlt_pipeline.ipynb

      development: true
